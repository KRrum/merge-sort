Sorting algorythm where i used 4 funcions:
1) def sort(v): - stars sorting algorythms without needeing more variables than needed, just the list that we are sorting
2) def merge_sort(v, l, r, t): - classic for merge sort divide and conquer with a slight twist. for small data it uses function number 4 - insersion sort which ussually
is faster for small data
3) def merge(v, p1, k1, p2, k2, t): - classic merge for merge sort, just merging to sorted lists into one
4) def insert_sort(v, l, r): - for small data it is faster that merge so it is here :D

How to use?
for a list use write sort(list)

